services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: habitos
      POSTGRES_PASSWORD: habitos
      POSTGRES_DB: habitos
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
